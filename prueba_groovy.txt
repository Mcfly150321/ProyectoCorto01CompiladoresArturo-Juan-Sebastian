def calcularTotal(precio, cantidad) {
    // Probando operadores y flotantes
    double impuesto = 1.15
    precio++
    
    boolean activo = true
    boolean vacio = false
    
    if (cantidad > 0 && activo) {
        return precio * cantidad * impuesto
    } else {
        return 0.0
    }
}